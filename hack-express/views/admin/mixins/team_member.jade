mixin team_member(team_id, user_id, name)
	tr
		td
			| #{name}
		td
			a(href="/admin/teams/#{team_id}/members/#{user_id}", data-method="delete", rel="nofollow").delete-button.btn.btn-danger.pull-right Remove