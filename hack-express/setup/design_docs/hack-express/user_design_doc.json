{
 "_id": "_design/users",
 "views": {
   "by_username": {
     "map": "function(doc) { if (doc.type == 'user') { emit(doc.username, { username: doc.username, password: doc.password }); }}"
    },
    "by_id": {
      "map": "function(doc) { if (doc.type == 'user') { emit(doc._id, doc); }}"
    }
  }
}